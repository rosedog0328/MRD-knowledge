# ── 絕對不可進版控：論文原始檔 ─────────────────────────────────
# 本知識庫的論文 PDF 全部放在另一個位置（MRD_detection/docs/paper/），
# 不在本 repo 內。這幾行是保險 —— 避免日後有人把 PDF 複製進來又 git add -A。
# 這條規則沿用 lab-tutorial 的教訓：原本只擋簡報格式，
# 結果一次 `git add -A` 就把未發表的論文 PDF 推上公開 repo。
*.pdf
*.pptx
*.ppt
*.key

# 從 lab-tutorial 複製工具鏈時會一併帶進來的投影片抽圖 ——
# 那是該實驗室未發表口試簡報的內容，不屬於本 repo，且本站一頁都沒用到。
site/assets/img/

# 定序資料與中間產物（本 repo 不該有，但同理保險）
*.bam
*.bai
*.vcf
*.vcf.gz
*.fastq
*.fastq.gz

# ── 系統與編輯器 ────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── 相依套件與建置產物 ──────────────────────────────────────────
node_modules/

# ── 祕密 ────────────────────────────────────────────────────────
.env
.env.*
!.env.example
